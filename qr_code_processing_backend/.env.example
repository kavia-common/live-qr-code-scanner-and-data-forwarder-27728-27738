# QR Code Processing Backend - Environment Variables (Example)

# External API endpoint that will receive decoded QR data
# Default at runtime: https://httpbin.org/post (if not provided)
EXTERNAL_API_URL=https://httpbin.org/post

# Optional Bearer token for the external API. If set, Authorization: Bearer <token> is added.
EXTERNAL_API_KEY=

# Webhook URL to forward the external API response
# Default at runtime: https://httpbin.org/post (if not provided)
WEBHOOK_URL=https://httpbin.org/post

# Webcam device index (0 is typically the default/primary camera)
CAMERA_INDEX=0

# Interval in milliseconds between frame scans (50-5000)
SCAN_INTERVAL_MS=250

# If true, do not resend identical QR payloads consecutively
SEND_UNIQUE_ONLY=true

# CORS allowed origins (comma-separated). Use "*" for all origins.
CORS_ALLOW_ORIGINS=*
